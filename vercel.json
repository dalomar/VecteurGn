{
  "version": 2,
  "buildCommand": "cd frontend && yarn install && yarn expo export --platform web",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
